name: Publish WAR to Nexus (Snapshots)

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  publish-to-nexus:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # Build WAR (pom.xml is in repo root)
      - name: Build WAR
        run: mvn -B clean package

      # Verify WAR file
      - name: Verify WAR file
        run: ls -l target

      # ✅ Correct version for maven-snapshots repo
      - name: Set version
        run: echo "VERSION=1.0.${{ github.run_number }}-SNAPSHOT" >> $GITHUB_ENV

      - name: Publish WAR to Nexus (Snapshots)
        run: |
          WAR_FILE=$(ls target/*.war)

          echo "Uploading $WAR_FILE with version $VERSION"

          mvn deploy:deploy-file \
            -Durl=${{ secrets.NEXUS_URL }} \
            -DrepositoryId=nexus \
            -Dfile=$WAR_FILE \
            -DgroupId=TrainBook \
            -DartifactId=TrainBook \
            -Dversion=${{ env.VERSION }} \
            -Dpackaging=war \
            -DgeneratePom=true \
            -Dusername=${{ secrets.NEXUS_USERNAME }} \
            -Dpassword=${{ secrets.NEXUS_PASSWORD }}
        env:
          MAVEN_OPTS: "-Dmaven.wagon.http.ssl.insecure=true -Dmaven.wagon.http.ssl.allowall=true"
